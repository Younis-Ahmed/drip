
type CardWrapperProps = {
    children: React.ReactNode;
    cardTitle: string;
    backBtnHref: string;
    backBtnLabel: string;
    showSocials?: boolean;
};

export const AuthCard:  = ({
    children,
    cardTitle,
    backBtnHref,
    backBtnLabel,
    showSocials,
}: CardWrapperProps) => {
    
}